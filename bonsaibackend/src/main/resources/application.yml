spring:
  application:
    name: bonsaibackend
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/bonsai
    username: postgres
    password: postgres
  jpa:
    hibernate:
      ddl-auto: update
management:
  port: 9001
  address: 127.0.0.1

